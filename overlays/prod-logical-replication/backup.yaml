apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: summit-logical-replica-backup
  namespace: summit-db-logical-replica
spec:
  immediate: true
  schedule: "0 0 15 * * *"
  backupOwnerReference: self
  cluster:
    name: summit-logical-replica
